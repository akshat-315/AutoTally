services:
  autotally:
    build: .
    container_name: autotally
    restart: unless-stopped
    ports:
      - "127.0.0.1:8001:8001"
    env_file:
      - .env
    volumes:
      - ./data:/app/server/data
      - ./data/logs:/app/server/logs
